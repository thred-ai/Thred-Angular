import { AddressEnsLookupPipe } from './address-ens-lookup.pipe';

describe('AddressEnsLookupPipe', () => {
  it('create an instance', () => {
    const pipe = new AddressEnsLookupPipe();
    expect(pipe).toBeTruthy();
  });
});
