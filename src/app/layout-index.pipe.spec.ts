import { LayoutIndexPipe } from './layout-index.pipe';

describe('LayoutIndexPipe', () => {
  it('create an instance', () => {
    const pipe = new LayoutIndexPipe();
    expect(pipe).toBeTruthy();
  });
});
