import { ColorShadePipe } from './color-shade.pipe';

describe('ColorShadePipe', () => {
  it('create an instance', () => {
    const pipe = new ColorShadePipe();
    expect(pipe).toBeTruthy();
  });
});
