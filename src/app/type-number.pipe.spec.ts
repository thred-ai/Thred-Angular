import { TypeNumberPipe } from './type-number.pipe';

describe('TypeNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
