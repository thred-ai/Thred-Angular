import { SliderValuePipe } from './slider-value.pipe';

describe('SliderValuePipe', () => {
  it('create an instance', () => {
    const pipe = new SliderValuePipe();
    expect(pipe).toBeTruthy();
  });
});
