import { LiveEarthViewPipePipe } from './live-earth-view-pipe.pipe';

describe('LiveEarthViewPipePipe', () => {
  it('create an instance', () => {
    const pipe = new LiveEarthViewPipePipe();
    expect(pipe).toBeTruthy();
  });
});
