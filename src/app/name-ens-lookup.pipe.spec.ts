import { NameEnsLookupPipe } from './name-ens-lookup.pipe';

describe('NameEnsLookupPipe', () => {
  it('create an instance', () => {
    const pipe = new NameEnsLookupPipe();
    expect(pipe).toBeTruthy();
  });
});
